name: ussd
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_phone_direct_caller: ^2.1.1
  google_fonts: ^6.2.1
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_flavorizr: ^2.2.3 

flutter:
  uses-material-design: true

flavorizr:
  ide: vscode
  flavors:
    vodafone:
      app:
        name: "Vodafone Codes"
        icon: "/var/mnt/fastD/design/Apps/ussd/vodafone.png"
      android:
        applicationId: "dev.lasheen.ussd.vodafone"

    orange:
      app:
        name: "Orange Codes"
        icon: "/var/mnt/fastD/design/Apps/ussd/orange.png"
      android:
        applicationId: "dev.lasheen.ussd.orange"

    we:
      app:
        name: "We Codes"
        icon: "/var/mnt/fastD/design/Apps/ussd/we.png"
      android:
        applicationId: "dev.lasheen.ussd.we"

    etisalat:
      app:
        name: "Etisalat Codes"
        icon: "/var/mnt/fastD/design/Apps/ussd/etisalat.png"  
      android:
        applicationId: "dev.lasheen.ussd.etisalat"
